<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="web.crawling.dao.RidibookDao">

	<select id="getRidibookList" resultType="web.crawling.vo.Ridibook">
		SELECT category, link, cover, title, author, star, starredPerson
		FROM RIDIBOOKS
	</select>
	
	<!-- SortingTable -->
	<select id="getRidibookListOrderByStarredPersonASC" resultType="web.crawling.vo.Ridibook">
		SELECT category, link, cover, title, author, star, starredPerson
		FROM RIDIBOOKS
		ORDER BY starredPerson DESC;
	</select>	
	<select id="getRidibookListOrderByStarredPersonDESC" resultType="web.crawling.vo.Ridibook">
		SELECT category, link, cover, title, author, star, starredPerson
		FROM RIDIBOOKS
		ORDER BY starredPerson ASC;
	</select>
	
	<insert id="insertRidibook" parameterType="web.crawling.vo.Ridibook">
		INSERT INTO RIDIBOOKS(category, link, cover, title, author, star, starredPerson)
		VALUES(#{category}, #{link}, #{cover}, #{title}, #{author}, #{star}, #{starredPerson})		
	</insert>
		 
</mapper>