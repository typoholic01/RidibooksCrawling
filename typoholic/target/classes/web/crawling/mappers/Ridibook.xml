<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="web.crawling.dao.RidibookDao">

	<select id="getRidibookList" resultType="web.crawling.model.Ridibook">
		SELECT category, rank, cover, title, author, star, starredPerson, link
		FROM RIDIBOOKS
	</select>
		 
</mapper>