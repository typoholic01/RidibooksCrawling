<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="PostFiles">

	<!-- 						CREATE 						-->
	<!-- 게시물 입력 -->
	<insert id="insertPostFile" useGeneratedKeys="false">
		INSERT INTO postfiles(
			postSeq,fileSeq
		)
		VALUES(
			currval('postSeq'), currval('fileSeq')
		)
	</insert>

	<!-- 						READ 						-->
	
	<!-- 						UPDATE 						-->
	
	<!-- 						DELETE 						-->
</mapper>