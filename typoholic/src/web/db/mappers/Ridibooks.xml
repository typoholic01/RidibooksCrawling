<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="Ridibook">

	<!-- getTotalCount -->
	<select id="getTotalBook" resultType="Integer">
		SELECT count(link)
		FROM ridibooks
	</select>
	
	<!-- getList with PL/SQL -->
	<select id="getRidibookList" resultType="Ridibook" parameterType="QueryBook">
		SELECT category, link, cover, title, author, star * starredPerson "clap"
		FROM ridibooks
		<if test="category != null and category != ''">
		WHERE category = #{category}
		</if>
		ORDER BY ${queryType} ${direction}
		LIMIT #{startListNum}, #{ListLimit}
	</select>
	
	<!-- SortingTable -->
	<select id="getRidibookListOrderByClapDESC" resultType="Ridibook" parameterType="web.view.util.Pagination">
		SELECT category, link, cover, title, author, star * starredPerson "clap"
		FROM ridibooks
		ORDER BY clap DESC
		LIMIT #{startArticle}, #{articleLimit}
	</select>
	<select id="getRidibookListOrderByClapASC" resultType="Ridibook">
		SELECT category, link, cover, title, author, star * starredPerson "clap"
		FROM ridibooks
		ORDER BY clap ASC
	</select>	
	
	<insert id="insertRidibook" parameterType="Ridibook">
		INSERT INTO ridibooks(category, link, cover, title, author, star, starredPerson)
		VALUES(#{category}, #{link}, #{cover}, #{title}, #{author}, #{star}, #{starredPerson})	
		ON DUPLICATE KEY UPDATE star=#{star}, starredPerson=#{starredPerson}	
	</insert>
		 
</mapper>